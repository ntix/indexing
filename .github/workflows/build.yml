name: Build

on:
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: npm install, build, test
      run: |
        npm i
        npm run build
        npm run test

